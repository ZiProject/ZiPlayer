import { GlobalPlayerRuntime } from "./GlobalPlayerRuntime";

/**
 * @deprecated Controller composition now belongs to GlobalPlayerRuntime.
 * Kept temporarily as a compatibility type for consumers importing the old symbol.
 */
export class PlayerRuntimeController extends GlobalPlayerRuntime {}
