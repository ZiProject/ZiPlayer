import { createAudioPlayer, NoSubscriberBehavior } from "@discordjs/voice";
import type { AudioPlayer } from "@discordjs/voice";
import type { PlayerOptions, TrackMiddleware } from "../types";
import type { PlayerManager } from "../structures/PlayerManager";
import { PlayerBus } from "../structures/PlayerBus";
import { PlayerAction } from "../structures/PlayerAction";
import { PlaybackOrchestrator } from "../structures/PlaybackOrchestrator";
import { TrackLoader } from "../structures/TrackLoader";
import { TrackResolver } from "../structures/TrackResolver";
import { PlaybackController } from "./PlaybackController";
import { StreamController } from "./StreamController";
import { QueueController } from "./QueueController";
import { AntiStuckController } from "./AntiStuckController";
import { TransitionController } from "./TransitionController";
import { PreloadController } from "./PreloadController";
import { ConnectionController } from "./ConnectionController";
import { LifecycleController } from "./LifecycleController";
import { ForwardController } from "./ForwardController";
import { Queue } from "../structures/Queue";
import { StreamManager } from "../structures/StreamManager";
import { PreloadManager } from "../structures/PreloadManager";
import type { Player } from "../structures/Player";
export interface PlayerRuntimeControllerOptions { player: Player; manager: PlayerManager; options: PlayerOptions; debug: (...args:any[])=>void; }
export class PlayerRuntimeController {
 public readonly bus=new PlayerBus(); public readonly actionExecutor=new PlayerAction(this.bus); public readonly connectionController:ConnectionController; public readonly lifecycleController:LifecycleController; public readonly forwardController:ForwardController; public readonly queue:Queue; public readonly audioPlayer:AudioPlayer; public readonly streamManager:StreamManager; public readonly preloadManager:PreloadManager; public readonly queueController:QueueController; public readonly trackLoader:TrackLoader; public readonly playbackController:PlaybackController; public readonly streamController:StreamController; public readonly antiStuckController:AntiStuckController; public readonly transitionController:TransitionController; public readonly preloadController:PreloadController; public readonly orchestrator:PlaybackOrchestrator; private disposed=false;
 public constructor(o:PlayerRuntimeControllerOptions){const {player,manager,options}=o;const middleware:TrackMiddleware[]=[...manager.getTrackMiddlewareChain(),...(Array.isArray(options.trackMiddleware)?options.trackMiddleware:options.trackMiddleware?[options.trackMiddleware]:[])];this.connectionController=new ConnectionController({guildId:player.guildId,bus:this.bus,options,debug:o.debug});this.lifecycleController=new LifecycleController({bus:this.bus,options,debug:o.debug});this.forwardController=new ForwardController(player,{debug:o.debug});this.queue=new Queue();this.audioPlayer=createAudioPlayer({behaviors:{noSubscriber:NoSubscriberBehavior.Pause,maxMissedFrames:100}});this.streamManager=new StreamManager({maxConcurrentStreams:options.maxStreamStore??4,streamTimeout:5*60*1000,maxListenersPerStream:15,enableMetrics:true,autoDestroy:true});this.queueController=new QueueController({queue:this.queue,bus:this.bus});const resolver=new TrackResolver(this.streamManager);this.preloadManager=new PreloadManager({streamManager:this.streamManager,debug:o.debug,getNextTrack:()=>this.queue.loop()==="track"?this.queue.currentTrack:this.queue.nextTrack,getStream:t=>resolver.resolve(player,t),removeTrackFromQueue:t=>{const n=this.queue.nextTrack;return n===t||(n?.id!==undefined&&t.id!==undefined&&n.id===t.id)||(n?.url!==undefined&&t.url!==undefined&&n.url===t.url)?this.queue.remove(0)!==null:false},isDestroyed:()=>this.disposed,isEnabled:()=>options.preload?.enabled??true});this.trackLoader=new TrackLoader({middleware,context:{player,manager},resolvers:[t=>resolver.resolve(player,t)],recovery:options.antiStuck,preloadManager:this.preloadManager,debug:o.debug});this.playbackController=new PlaybackController({audioPlayer:this.audioPlayer,bus:this.bus});this.streamController=new StreamController({streamManager:this.streamManager,bus:this.bus});this.antiStuckController=new AntiStuckController({...options.antiStuck,bus:this.bus});this.transitionController=new TransitionController({enabled:options.crossfade?.enabled??true,durationMs:options.crossfade?.durationMs,smartEnabled:options.smartTransition?.enabled??true,genreAware:options.smartTransition?.genreAware??true,beatAlign:options.smartTransition?.beatAlign??true,baseDurationMs:options.smartTransition?.baseDurationMs??options.crossfade?.durationMs,minDurationMs:options.smartTransition?.minDurationMs,maxDurationMs:options.smartTransition?.maxDurationMs,beatAlignMaxWaitMs:options.smartTransition?.beatAlignMaxWaitMs,genreDurations:options.smartTransition?.genreDurations});this.preloadController=new PreloadController({loader:this.trackLoader,manager:this.preloadManager,bus:this.bus});this.orchestrator=new PlaybackOrchestrator(this.bus,{trackLoader:this.trackLoader,streamController:this.streamController,playbackController:this.playbackController,queueController:this.queueController,antiStuckController:this.antiStuckController,transitionController:this.transitionController,preloadController:this.preloadController});this.bus.publish("initialized");this.bus.publish("ready");}
 public dispose():void{if(this.disposed)return;this.disposed=true;this.lifecycleController.dispose();this.forwardController.dispose();void this.connectionController.dispose();this.actionExecutor.dispose();this.orchestrator.dispose();this.preloadController.dispose();this.preloadManager.cancelPreload();this.streamController.dispose();this.streamManager.destroyAll();this.queueController.dispose();this.playbackController.dispose();this.bus.publish("destroyed");this.bus.clear();}
}